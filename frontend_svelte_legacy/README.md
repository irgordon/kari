# Legacy Svelte Frontend (Archived)

This directory contains the previous SvelteKit frontend implementation.

It has been archived due to dependency instability and resolution issues that made the frontend unreliable to build and maintain.

It is retained strictly for historical reference during migration and should not receive new feature work.

The active frontend has moved to `/frontend` and is now React + Vite based.
